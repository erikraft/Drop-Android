# Sample keystore.properties — DO NOT commit your real keystore or passwords
# Put a file named `keystore.properties` at the project root with the real values
# Example:
# storeFile=keystores/upload-keystore.jks
# storePassword=your_store_password
# keyAlias=upload
# keyPassword=your_key_password

# Values below are placeholders — copy and edit for your environment

storeFile=keystores/upload-keystore.jks
storePassword=erikraft220709#
keyAlias=erikraft
keyPassword=erikraft220709#

